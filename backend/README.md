# Backend

Kotlin Spring Boot API for Finanzlotse.

## Local Run

```bash
mvn spring-boot:run
```

The API exposes a public health endpoint at:

```text
GET /api/health
```

Swagger UI will be available at:

```text
http://localhost:8080/swagger-ui.html
```

